workflows:
  ios-build:
    name: iOS Build
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Install dependencies
        script: flutter pub get
      - name: Build iOS app
        script: flutter build ios
      - name: Build API
        script: echo "Build your API here"
    artifacts:
      - build/ios/**/Runner.ipa
